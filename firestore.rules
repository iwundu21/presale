rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{walletAddress} {
      allow read, write: if true; // In production, lock this down further
      
      match /transactions/{transactionId} {
        allow read, write: if true; // In production, lock this down further
      }
    }
  }
}