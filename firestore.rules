rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes to all collections for development.
    // This should be secured before launching to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
